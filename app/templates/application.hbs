<header class="top-bar">
  <div class="container">
    <h1>Address Book</h1>
    {{link-to "Add a Contact" "contacts.new" tagName="button"}}
  </div>
</header>

<main>
  {{#each flashMessages.queue as |flash|}}
    {{flash-message flash=flash}}
  {{/each}}

  {{outlet}}
</main>
